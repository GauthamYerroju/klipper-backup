#####################################################################
#   A better print_start macro for v2/trident
#####################################################################

## *** THINGS TO UNCOMMENT: ***
## Bed mesh (2 lines at 2 locations)
## Nevermore (if you have one)
## Z_TILT_ADJUST (For Trident only)
## QUAD_GANTRY_LEVEL (For V2.4 only)
## Beacon Contact logic (if you have one. 4 lines at 4 locations)

[delayed_gcode _turn_off_carbon_filter]
initial_duration: 0
gcode:
    {% if printer["temperature_sensor chamber_temp"].temperature <= 27 %}
        SET_DISPLAY_TEXT MSG="Turning off carbon filter."
        SET_FAN_SPEED FAN=Carbon_Filter SPEED=0
    {% else %}
        SET_DISPLAY_TEXT MSG="Waiting for chamber to cool down. Current: {printer["temperature_sensor chamber_temp"]}, Target: 27"
        UPDATE_DELAYED_GCODE ID=_turn_off_carbon_filter DURATION=10
    {% endif %}

[gcode_macro turn_off_filter_after_chamber_cools]
gcode:
  UPDATE_DELAYED_GCODE ID=_turn_off_carbon_filter DURATION=1

[gcode_macro PRINT_START]
gcode:
  # This part fetches data from your slicer. Such as bed, extruder, and chamber temps and size of your printer.
  {% set target_bed = params.BED|int %}
  {% set target_extruder = params.EXTRUDER|int %}
  {% set target_chamber = params.CHAMBER|default("45")|int %}
  {% set x_wait = printer.toolhead.axis_maximum.x|float / 2 %}
  {% set y_wait = printer.toolhead.axis_maximum.y|float / 2 %}

  # Clear any lingering pause states
  CLEAR_PAUSE

  # Home the printer, set absolute positioning and update the Stealthburner LEDs.
  G28 X Y                                               # Home X and Y
  G28 Z                                                 # Home Z
  G90                                                   # Absolute position

  # Uncomment for bed mesh (1 of 2 for bed mesh)
  BED_MESH_CLEAR                                        # Clear old saved bed mesh (if any)

  # Check if the bed temp is higher than 90c - if so then trigger a heatsoak.
  {% if params.BED|int > 90 %}
    SET_DISPLAY_TEXT MSG="Bed: {target_bed}c"           # Display info on display
    M106 S255                                           # Turn on the PT-fan

    G1 X{x_wait} Y{y_wait} Z15 F9000                    # Go to center of the bed
    # M190 S{target_bed}                                  # Set the target temp for the bed
    SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET={target_bed}
    SET_DISPLAY_TEXT MSG="Heatsoak: {target_chamber}c"  # Display info on display
    TEMPERATURE_WAIT SENSOR="temperature_sensor chamber_temp" MINIMUM={target_chamber}   # Waits for chamber temp

  # If the bed temp is not over 90c, then skip the heatsoak and just heat up to set temp with a 5 min soak
  {% else %}
    SET_DISPLAY_TEXT MSG="Bed: {target_bed}c"           # Display info on display
    G1 X{x_wait} Y{y_wait} Z15 F9000                    # Go to center of the bed
    # M190 S{target_bed}                                  # Set the target temp for the bed
    SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET={target_bed}
    # SET_DISPLAY_TEXT MSG="Soak for 5 min"               # Display info on display
    # G4 P300000                                          # Wait 5 min for the bedtemp to stabilize
  {% endif %}

  TEMPERATURE_WAIT SENSOR="heater_bed" MINIMUM={target_bed}

  # Turn on the carbon filter before heating up the hotend.
  SET_FAN_SPEED FAN=Carbon_Filter SPEED=0.98
  # Heat hotend to 150c. This helps with getting a correct Z-home.
  SET_DISPLAY_TEXT MSG="Hotend: 150c"                   # Display info on display
  # M109 S150                                             # Heat hotend to 150c
  SET_HEATER_TEMPERATURE HEATER=extruder TARGET=150
  TEMPERATURE_WAIT SENSOR="extruder" MINIMUM=150

  
  ## Uncomment for V2.4 (Quad gantry level AKA QGL)
  SET_DISPLAY_TEXT MSG="Leveling"                      # Display info on display
  QUAD_GANTRY_LEVEL                                    # Level the printer via QGL
  G28 Z                                                # Home Z again after QGL

  ## Uncomment for bed mesh (2 of 2 for bed mesh)
  SET_DISPLAY_TEXT MSG="Bed mesh"                      # Display info on display
  BED_MESH_CALIBRATE ADAPTIVE=1                        # Start the bed mesh (add ADAPTIVE=1) for adaptive bed mesh

  # BED_MESH_PROFILE LOAD=default

  # Heat up the hotend up to target via data from slicer
  SET_DISPLAY_TEXT MSG="Hotend: {target_extruder}c"     # Display info on display
  G1 X{x_wait} Y{y_wait} Z15 F9000                      # Go to center of the bed
  M107                                                  # Turn off partcooling fan
  # M109 S{target_extruder}                               # Heat the hotend to set temp
  SET_HEATER_TEMPERATURE HEATER=extruder TARGET={target_extruder}
  TEMPERATURE_WAIT SENSOR="extruder" MINIMUM={target_extruder}

  # Get ready to print by doing a primeline and updating the LEDs
  SET_DISPLAY_TEXT MSG="Printer goes brr"               # Display info on display
  G90                                                   # Absolute position
  G0 X0.5 Y0.5 Z0.4 F10000                           # Go to starting point
  G1 X0.5 Y20 E20 F1000                                    # Primeline

[gcode_macro PRINT_END]
gcode:
  # Life Z relatively
  G91
  G1 Z25
  G90

  # Park the toolhead
  G1 X0 F6000
  G1 Y{printer.toolhead.axis_maximum.y} F6000

  # Filament retract to prevent oozing
  G92 E0                                      # Reset extruder position
  G1 E-2 F1500                                # Retract 2 mm

  # Turn off heaters and fans
  M104 S0                                     # Turn off hotend
  M140 S0                                     # Turn off bed
  M107                                        # Turn off part cooling fan

  # Trigger turning off carbon filter when chamber temp reaches room temp
  turn_off_filter_after_chamber_cools

  # Disable steppers (optional – remove if you want to keep motors locked)
  M84                                         # Disable motors
